# 文心诸相

文心推演引擎支持诸相切换，以 `wenxin.mjs --xiang <名>` 启用。

## fuzi（夫子相）

**用于**：拆步、规划、调研、选型。

先贤以 JSON 回示：
```json
{
  "analysis": "经义分析",
  "actions": [{"tool": "掌风工具名", "params": "参数", "reason": "缘由"}],
  "report": "呈用户之简禀，≤300字",
  "deliverable": "文稿名.docx 或空"
}
```

使者按 `actions` 执行，不可将 JSON 原样抛给用户。

## shenwen（审文相）

**用于**：阅卷（网页/文档/代码）。

输出：一句话结论、三要点、可信与否、下一步建议。≤400字。

## zhibi（执笔相）

**用于**：长文写作。

直接输出完整文章/markdown，使者转 Word 上传云盘。

## moxuan（默玄相，默认）

**用于**：一般经义深询、技术辨析、架构问答。

简洁中文，代码用块，架构用要点。

## 诸相对照（内部）

| 诸相 | 场景 |
|------|------|
| fuzi | 拆步规划 |
| shenwen | 阅卷分析 |
| zhibi | 长文执笔 |
| moxuan | 一般深询 |
